//import html2pdf from 'html2pdf.js';

/*
  const estimateRef = useRef();

  // function to save estimate as a PDF
  const saveEstimateAsPDF = () => {
    const options = {
      filename: 'estimate.pdf', // name of PDF file
      image: { type: 'jpeg', quality: 0.98 }, // image quality settings
      html2canvas: { scale: 4 }, // rendering quality
      jsPDF: { unit: 'mm', format: 'a4', orientation: 'portrait' } // PDF settings
    };
    
    // use html2pdf to generate PDF from the selected element
    html2pdf()
      .from(estimateRef.current) // capture the div using the ref
      .set(options) // set options for the PDF
      .save(); // trigger the download of the PDF
  };
    */

    {/* Save as PDF Button */}
    {/*<button onClick={saveEstimateAsPDF}>Save as PDF</button>*/}